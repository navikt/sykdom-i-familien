import React from 'react';
import { Panel } from 'nav-frontend-paneler';

interface Props {}

const OmsorgsdagerKalkulator: React.FunctionComponent<Props> = (props) => (
    <section>
        <h2>Omsorgspenger kalkulator</h2>
        <Panel>Hva kommer her?</Panel>
    </section>
);

export default OmsorgsdagerKalkulator;
