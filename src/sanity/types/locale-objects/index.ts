import { StringBlockValue } from '../parts';

export interface LocaleStringType {
    nb: string;
    nn: string;
}

export interface LocaleSimpleTextType {
    nb: string;
    nn: string;
}

export interface LocaleRichTextType {
    nb: StringBlockValue;
    nn: StringBlockValue;
}
